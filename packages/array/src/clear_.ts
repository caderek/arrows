type Clear_ = <T>(arr: T[]) => []

/**
 * Creates a new, empty array.
 *
 * @param arr Initial array
 * @returns New array
 */
const clear_: Clear_ = (arr) => []

export { clear_ }
export default clear_
