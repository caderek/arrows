import getType from './getType'
import identity from './identity'
import is from './is'
import types from './types'

export { getType, identity, is, types }

export default {
  getType,
  identity,
  is,
  types,
}
