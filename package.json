{
  "name": "root",
  "scripts": {
    "boot": "node scripts/execute install",
    "build": "node scripts/execute build",
    "push": "node scripts/execute publish",
    "test": "node scripts/execute test",
    "lint": "node scripts/execute lint",
    "coverage": "node scripts/execute coverage"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.8",
    "chalk": "^2.4.2",
    "fs-extra": "^8.1.0"
  }
}
